const params = {
  input: 'input',
  output: 'output',
};

const fileExtensions = {
  html: '.html',
  css: '.css',
};

const minArgsQuantity = 4;

const baseOutputFilePrefix = 'inlined';

module.exports = {
  params,
  minArgsQuantity,
  fileExtensions,
  baseOutputFilePrefix,
};
